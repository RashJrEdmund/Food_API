const auth_route = require("./auth")
const food_data_route = require("./food_data");
const users_route = require("./users");

module.exports = {
    auth_route,
    food_data_route,
    users_route,
}
